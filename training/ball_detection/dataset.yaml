# Volleyball Ball Detection — Dataset Config

path: ../../training/datasets/ball_detection
train: train/images
val:   val/images

nc: 1
names:
  0: volleyball

# Notes:
# - Use imgsz=1280 (ball is tiny, needs resolution)
# - Synthetic samples generated by download_datasets.py --task ball
# - Supplement with real volleyball footage labels for best results
# - Recommended: 500+ real labelled frames for production quality
